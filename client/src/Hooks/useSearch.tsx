import axios from "axios";
import React from "react";

const useSearch = ({ id, param }) => {
  const [searchParam, setSearchParam] = useState("");
  useEffect(() => {
    axios.post().then().catch();
  }, []);
  return <></>;
};

export default useSearch;
